function sortList(e) {
  var t, r, i, n;
  for (r = TRUE; r; ) {
    for (
      r = FALSE, i = e.getElementsByTagName("li"), t = 0;
      t < i.length - 1;
      t++
    )
      if (
        ((n = FALSE),
        i[t].innerHTML.toLowerCase() > i[t + 1].innerHTML.toLowerCase())
      ) {
        n = TRUE;
        break;
      }
    n && (i[t].parentNode.insertBefore(i[t + 1], i[t]), (r = TRUE));
  }
}
function list(e, t) {
  for (let r = 0; r < t.children.length; r++)
    (t.children[r].children[0].innerText = t.children[r].children[0]
      .getAttribute("href")
      .split(e)[0]),
      t.children[r].setAttribute(
        "class",
        t.children[r].children[0].getAttribute("href").split(e)[1]
      );
}
